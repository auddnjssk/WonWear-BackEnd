<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="ItemMapper">

  <select id="selectItem" parameterType="Long" resultType="Map">
	SELECT * 
	FROM T_ITEMS 
	WHERE CATE_NO = #{cateNo} OR CATE_NO IN (SELECT CATE_NO FROM T_CATEGORY WHERE PARENT_CATE_NO = #{cateNo})
  </select>
  
  <delete id="deleteItem" parameterType="java.util.List" >
	DELETE FROM T_ITEMS 
	WHERE item_id IN
	<foreach item="item_id" collection="list" open="(" separator="," close=")">
    	#{item_id}
  	</foreach>
  
  </delete>
  
</mapper>
